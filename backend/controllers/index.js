const userController = require('./user');
const salaController = require('./sala');
const locController = require('./loc');

module.exports = {
    userController,
    salaController,
    locController,
};
